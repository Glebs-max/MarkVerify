<UserControl x:Class="WpfApp_IC.Pages.VideojetErrors"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:WpfApp_IC.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:VideojetErrorsViewModel}"
             mc:Ignorable="d" Background="#F4F4F4" FontSize="24">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Margin="10" HorizontalAlignment="Stretch" VerticalAlignment="Top" Tag="{Binding Errors.Count, Converter={StaticResource VideojetErrorsConverter}}" Visibility="{Binding Tag.AllGoodVisibility, RelativeSource={RelativeSource Self}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Image Source="/Assets/good.png" Height="40" Margin="0,0,20,0" Stretch="Uniform"/>
            <TextBlock Grid.Column="1" Text="Всё хорошо" Margin="0,0,20,0" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
        <ListView ScrollViewer.HorizontalScrollBarVisibility="Disabled" ItemsSource="{Binding Errors}" Tag="{Binding Errors.Count, Converter={StaticResource VideojetErrorsConverter}}" Visibility="{Binding Tag.ErrorListVisibility, RelativeSource={RelativeSource Self}}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5" HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Image Source="{Binding ErrorType, Converter={StaticResource ErrorTypeConverter}}" Height="40" Margin="0,0,20,0" Stretch="Uniform"/>
                        <TextBlock Grid.Column="1" Text="{Binding Code}" Margin="0,0,20,0" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="{Binding Title}" Margin="0,0,20,0" TextWrapping="Wrap" FontSize="18" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button x:Name="ClearErrors" Grid.Row="1" Content="Сбросить ошибки" Height="50" Margin="20,20" Click="ClearErrors_Click"/>
    </Grid>
</UserControl>
