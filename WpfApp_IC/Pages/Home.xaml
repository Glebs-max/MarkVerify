<UserControl x:Class="WpfApp_IC.Pages.Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WpfApp_IC.Pages"
             xmlns:viewmodels="clr-namespace:WpfApp_IC.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:HomeViewModel}"
             mc:Ignorable="d">
    <Grid Background="#ff9e59">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Image Source="/Assets/smart logo.png" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10" MaxWidth="600" Opacity="0.75"/>
            <Image Source="/Assets/fair mark.png" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="10" MaxWidth="600" Opacity="0.75" Grid.Column="1"/>
        </Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical">
            <Button Content="Старт" Width="400" Height="100" FontSize="40" Margin="10" Background="White" Click="Start_Click"/>
            <Button Content="Настройки" Width="400" Height="100" FontSize="40" Margin="10" Background="White" Click="Settings_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
