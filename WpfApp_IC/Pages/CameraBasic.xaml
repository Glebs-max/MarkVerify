<UserControl x:Class="WpfApp_IC.Pages.CameraBasic"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:WpfApp_IC.ViewModels"
             xmlns:conv="clr-namespace:WpfApp_IC.Converters"
             d:DataContext="{d:DesignInstance Type=viewmodels:CameraViewModel}"
             mc:Ignorable="d" FontSize="14">

    <Grid Grid.Column="2">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- DataMatrix -->
        <TextBox Height="30"
                 Margin="10,5"
                 FontSize="19"
                 IsReadOnly="True"
                 VerticalContentAlignment="Center"
                 Background="#FFF9F9F9"
                 Text="{Binding DataMatrix}"
                 Foreground="{Binding DataMatrixBrush}"/>

        <!-- Изображение -->
        <Grid Grid.Row="1" Margin="10,5">
            <Border BorderBrush="#CCC" BorderThickness="1">
                <Image Stretch="Uniform" Source="{Binding Frame}"/>
            </Border>
            <Image Source="/Assets/snapshot.png" Width="35" Margin="5" Cursor="Hand" HorizontalAlignment="Left" VerticalAlignment="Top" IsEnabled="{Binding IsRunning}" MouseDown="TriggerManual_Click"/>
            <Image Source="{Binding IsRunning, Converter={StaticResource InspectionStatusConverter}}" Width="25" Margin="5" HorizontalAlignment="Right" VerticalAlignment="Top"/>
        </Grid>
    </Grid>
</UserControl>
