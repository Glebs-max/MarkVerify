<UserControl x:Class="WpfApp_IC.Pages.CameraAdvanced"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:WpfApp_IC.ViewModels"
            
             mc:Ignorable="d">
 <!--d:DataContext="{d:DesignInstance Type=viewmodels:CameraAdvancedViewModel}"-->
   <!-- <Grid Background="#FF2B2B2B" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Камера — расширенный режим"
                   Foreground="White"
                   FontSize="22"
                   Margin="0,0,0,10"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            --><!-- Отбраковка --><!--
            <Border Grid.Column="0"
                    Margin="0,0,10,0"
                    Background="#FF3A3A3A"
                    CornerRadius="8"
                    Padding="15">
                <StackPanel>
                    <TextBlock Text="Отбраковка"
                               Foreground="White"
                               FontSize="18"
                               Margin="0,0,0,10"/>

                    <TextBlock Text="Задержка (мс)"
                               Foreground="LightGray"
                               FontSize="14"/>

                    <TextBox Text="{Binding RejectDelayMs}"
                             Width="120"
                             FontSize="16"
                             Margin="0,5,0,10"/>

                    <Button Content="Сохранить задержку"
                            Width="200"
                            Height="35"
                            Margin="0,5,0,0"
                            Click="SaveRejectDelay_Click"/>
                </StackPanel>
            </Border>

            --><!-- IDMVS --><!--
            <Border Grid.Column="1"
                    Background="#FF3A3A3A"
                    CornerRadius="8"
                    Padding="15">
                <StackPanel>
                    <TextBlock Text="Интеграция"
                               Foreground="White"
                               FontSize="18"
                               Margin="0,0,0,10"/>

                    <TextBlock Text="Путь к IDMVS"
                               Foreground="LightGray"
                               FontSize="14"/>

                    <DockPanel Margin="0,5,0,10">
                        <TextBox Text="{Binding IdmvsPath}"
                                 FontSize="14"
                                 Width="300"
                                 Margin="0,0,10,0"/>

                        <Button Content="Обзор"
                                Width="80"
                                Height="30"
                                Click="BrowseIDMVS_Click"/>
                    </DockPanel>

                    <Button Content="Открыть IDMVS"
                            Width="200"
                            Height="35"
                            Margin="0,5,0,10"
                            Click="OpenIDMVS_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>-->
</UserControl>
