<UserControl x:Class="WpfApp_IC.Pages.ParameterInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:models="clr-namespace:LabelDesigner.Models;assembly=LabelDesigner"
             d:DataContext="{d:DesignInstance Type=models:Field}"
             mc:Ignorable="d" Background="#F4F4F4" FontSize="24">
    <ContentPresenter Content="{Binding}" Margin="4">
        <ContentPresenter.Resources>
            <DataTemplate DataType="{x:Type models:TextField}">
                <StackPanel>
                    <TextBlock Text="{Binding FieldName}"/>
                    <TextBox Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </DataTemplate>
            <DataTemplate DataType="{x:Type models:ImageField}">
                <StackPanel>
                    <TextBlock Text="{Binding FieldName}"/>
                    <TextBox Text="{Binding Source, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True"/>
                    <Button Content="Browse" HorizontalAlignment="Right" Margin="0,5,0,0" Padding="5" Click="Browse_Click"/>
                </StackPanel>
            </DataTemplate>
            <DataTemplate DataType="{x:Type models:DateField}">
                <StackPanel>
                    <TextBlock Text="{Binding FieldName}"/>
                    <DatePicker SelectedDate="{Binding Date, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </DataTemplate>
            <DataTemplate DataType="{x:Type models:BarcodeField}">
                <StackPanel>
                    <TextBlock Text="{Binding FieldName}"/>
                    <TextBox Text="{Binding BarcodeData, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </DataTemplate>
        </ContentPresenter.Resources>
    </ContentPresenter>
</UserControl>
