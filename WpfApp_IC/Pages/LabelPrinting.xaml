<UserControl x:Class="WpfApp_IC.Pages.LabelPrinting"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:WpfApp_IC.ViewModels"
             xmlns:designer="clr-namespace:LabelDesigner;assembly=LabelDesigner"
             d:DataContext="{d:DesignInstance Type=vm:LabelPrintingViewModel}"
             mc:Ignorable="d" Background="#F4F4F4" FontSize="24">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.Tag>
                <MultiBinding Converter="{StaticResource PrintingStatusConverter}">
                    <Binding Path="PrintingStatus"/>
                    <Binding Path="ErrorStatus"/>
                </MultiBinding>
            </Grid.Tag>
            <Grid.Background>
                <Binding Path="Tag.StatusColor" RelativeSource="{RelativeSource Self}"/>
            </Grid.Background>
            <TextBlock x:Name="StatusBar" TextAlignment="Center" VerticalAlignment="Center" Padding="6" FontSize="32" FontFamily="Bahnschrift">
                <TextBlock.Tag>
                    <MultiBinding Converter="{StaticResource PrintingStatusConverter}">
                        <Binding Path="PrintingStatus"/>
                        <Binding Path="ErrorStatus"/>
                    </MultiBinding>
                </TextBlock.Tag>
                <TextBlock.Text>
                    <Binding Path="Tag.StatusText" RelativeSource="{RelativeSource Self}"/>
                </TextBlock.Text>
            </TextBlock>
            <Image Height="50" Margin="5,5,20,5" HorizontalAlignment="Right" Cursor="Hand">
                <Image.Tag>
                    <MultiBinding Converter="{StaticResource PrintingStatusConverter}">
                        <Binding Path="PrintingStatus"/>
                        <Binding Path="ErrorStatus"/>
                    </MultiBinding>
                </Image.Tag>
                <Image.Source>
                    <Binding Path="Tag.StatusIcon" RelativeSource="{RelativeSource Self}"/>
                </Image.Source>
            </Image>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <GridSplitter Grid.Column="1"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          ResizeBehavior="PreviousAndNext"
                          ShowsPreview="True"
                          Background="MediumVioletRed"
                          Width="5"/>
            <designer:DesignerCanvas x:Name="LabelArea" Background="White" Preview="True" Panel.ZIndex="-1" DataContext="{Binding DesignerViewModel}"/>
            
        </Grid>
        <WrapPanel Grid.Row="2" HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal" Margin="25,20">
                <TextBlock Text="Очередь: "/>
                <TextBlock Text="{Binding VideojetPrinter.QueueSize}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="25,20">
                <TextBlock Text="Остаток: "/>
                <TextBlock Text="{Binding GTIN.CountAviable}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="25,20">
                <TextBlock Text="Верифицировано: "/>
                <TextBlock Text="{Binding Verified}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="25,20">
                <TextBlock Text="Отбраковано: "/>
                <TextBlock Text="{Binding Rejected}"/>
            </StackPanel>
        </WrapPanel>
        <WrapPanel Grid.Row="3" HorizontalAlignment="Center">
            <Button x:Name="Stop" Content="Завершить" Width="150" Height="50" FontSize="24" Margin="25,20" Click="Stop_Click"/>
            <Button x:Name="Pause" Content="Пауза" Width="150" Height="50" FontSize="24" Margin="25,20" Click="Pause_Click"/>
            <Button x:Name="Report" Content="Отчёт" Width="150" Height="50" FontSize="24" Margin="25,20" IsEnabled="False"/>
            <Button x:Name="Exit" Content="Выход" Width="150" Height="50" FontSize="24" Margin="25,20" IsEnabled="False" Click="Exit_Click"/>
        </WrapPanel>
    </Grid>
</UserControl>
