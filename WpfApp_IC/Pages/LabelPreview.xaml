<UserControl x:Class="WpfApp_IC.Pages.LabelPreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:pages="clr-namespace:WpfApp_IC.Pages"
             xmlns:vm="clr-namespace:WpfApp_IC.ViewModels"
             xmlns:designer="clr-namespace:LabelDesigner;assembly=LabelDesigner"
             d:DataContext="{d:DesignInstance Type=vm:LabelPreviewViewModel}"
             mc:Ignorable="d" Background="#F4F4F4" FontSize="24">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="7*" MinWidth="150"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="3*" MinWidth="150"/>
            </Grid.ColumnDefinitions>
            <designer:DesignerCanvas Preview="True" Background="White" Panel.ZIndex="-1" DataContext="{Binding DesignerViewModel}"/>
            <GridSplitter Grid.Column="1"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      ResizeBehavior="PreviousAndNext"
                      ShowsPreview="True"
                      Background="MediumVioletRed"
                      Width="5"/>
            <pages:DataInput Grid.Column="2" DataContext="{Binding DesignerViewModel}"/>
        </Grid>
        <Button Content="Запуск печати" Height="50" Margin="0,10,0,0" Click="PrintLabel_Click" Grid.Row="1"/>
        <Button Content="Назад" Height="50" Margin="0,10,0,0" Click="GetBack_Click" Grid.Row="2"/>
    </Grid>
</UserControl>
